var operatorFunction = {
    '+' : function(x, y) {
        return x + y;
    },
    '-' : function(x, y) {
        return x - y;
    },
    '*' : function(x, y) {
        return x * y;
    }
};


var add= '+' ;


function counting(x,y){
console.log(x ,add , y)
}

counting(4,6)
